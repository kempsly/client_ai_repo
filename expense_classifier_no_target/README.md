# LLM Expense Classifier API

This API uses an LLM to predict G/L account numbers based on expense details.

## Setup

1. Clone the repository.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
